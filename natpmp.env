# /etc/natpmp.env — credentials for natpmp.service
# Permissions must be 600 (readable only by root):
#   sudo chmod 600 /etc/natpmp.env

QBT_URL=http://localhost:8080
QBT_USER=admin
QBT_PASS=changeme
